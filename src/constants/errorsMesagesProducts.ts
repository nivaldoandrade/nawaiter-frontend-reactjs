const errorsMessagesProducts = {
  'Cannot delete product with active orders':
    'Não é possível excluir o produto enquanto houver um pedido ativo associado',
} as { [key: string]: string };

export default errorsMessagesProducts;
