const errorsMessagesCompletedOrders = {
  'The order contains items that have not been delivered':
    'O pedido contém itens que não foram entregues',
} as { [key: string]: string };

export default errorsMessagesCompletedOrders;
