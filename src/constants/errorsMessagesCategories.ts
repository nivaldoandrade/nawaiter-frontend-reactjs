const errorsMessagesCategories = {
  'Cannot delete the category as it is being used by one or more products':
    'Não é possível excluir a categoria porque está sendo utilizada por um ou mais produtos',
} as { [key: string]: string };

export default errorsMessagesCategories;
